# Database Service Requirements
# Extends requirements/base.txt - only service-specific dependencies here
# Core dependencies (fastapi, uvicorn, pydantic, requests, pyyaml, sqlalchemy, 
# cryptography, gunicorn, etc.) are inherited from the base image

# Additional HTTP client for async operations
httpx==0.28.1
